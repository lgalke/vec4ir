language: python
python:
    - 3.6

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y python3-dev

install:
    - pip install -e .
    - pip install pyemd

script:
    pytest
